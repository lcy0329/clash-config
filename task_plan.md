# 代理规则优化计划

目标：修复规则源更新失败、补全 AI 分流、收紧节点筛选，并验证手机与桌面配置一致性。

## 阶段
1. 核对客户端语法与可用规则源：完成（Stash 运行时验收留在手机）。
2. 优化两端规则、节点策略及使用文档：完成。
3. 增加可重复验证，检查远程资源和核心加载：完成。
4. 复核差异、整理交付说明：完成；通过检查后提交并同步 origin/main。

## 实施原则
- 保留现有分流架构和手工配置，不引入生成器或新依赖。
- AI 使用明确服务域名，避免整段 ASN 和通用第三方域名误分流。
- 两端 AI 策略统一为手动选择；验证空候选时的行为。
- 远程更新周期保持 24 小时；下载成功与手机生效分别说明。
- 手机实际请求验证需要设备端执行，不能用静态检查冒充。

## 错误与处理
- 验证脚本原生检查的 HTTP 客户端变量作用域错误：已移至轮询块外，重新验证。

- 在线验证首次发现原 ProxyMedia.list 含 URL-REGEX 和 PROCESS-NAME，超出域名/IP 检查范围；进一步检查内核兼容性并评估纯流媒体替代源。
- 替代源调查：不存在 Clash/Ruleset/ProxyMedia.list（404）；GlobalMedia.yaml 仍混入 AI 且域名条目未完整包含，不采用。

## 验收结果
- 静态校验：两端 74 条规则、15 个远程规则集一致，引用完整，节点筛选正反例通过。
- 在线校验：15/15 源下载与解析成功，37 个域名分流用例通过。
- Mihomo v1.19.29：配置校验、所有规则集条数、AI 正常/无匹配/无节点场景通过。
- 回归有效性：验证脚本可拒绝优化前配置。
- iPhone/Stash 实机请求、空组行为及真实节点可用性未在本机验证；验收步骤已写入 README。
